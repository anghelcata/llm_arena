# =======================
# Duel-Arena .env (clean)
# =======================

# --- App ---
APP_NAME=Duel-Arena
ENV=dev

# --- Deterministic inference ---
TEMPERATURE=0.0
TOP_P=1.0
NUM_CTX=8192
SEED=42

# --- Provider (local via Ollama) ---
PROVIDER=ollama
OLLAMA_BASE_URL=http://localhost:11434
# Note: High timeout because committee runs can be long.
TIMEOUT_S=4200

# --- Committee of graders ---
# Comma-separated list, no spaces around colons.
COMMITTEE_MODELS=llama3.1:8b,zephyr:7b-beta,mistral:7b-instruct,gemma:7b-instruct
# One of: median | mean | trimmed
AGGREGATOR=median


ATTACKER_LLM_A1=openhermes:latest
ATTACKER_LLM_A2=openhermes:latest
LLM_JUDGE=dolphin3:8b

# --- Dataset & export ---
# IMPORTANT: Current code still expects the canonical filename.
DATA_PATH=./data/ML_v1_prep_gold.json
EXPORT_JSONL=./results/committee_scores.jsonl

# --- Neo4j (optional; used if graph persistence is enabled) ---neo4j://127.0.0.1:7687
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=password
NEO4J_DATABASE=duel-arena
NEO4J_ENCRYPTED=false

# --- Runner selection (optional filters; empty = unused) ---
# Exact IDs to include (CSV). Leave empty to include all.
RUN_INCLUDE_IDS=
# Exact IDs to exclude (CSV).
RUN_EXCLUDE_IDS=
# Regex to include IDs (applied after include/exclude lists).
RUN_ID_REGEX=
# Random subsampling (0 = off).
RUN_SAMPLE_N=0
# Hard cap on processed items (0 = no cap).
RUN_MAX_ITEMS=0
# Shuffle order (0/1).
RUN_SHUFFLE=0
# Shuffle seed when RUN_SHUFFLE=1.
RUN_SHUFFLE_SEED=42

# --- Corruption success thresholds (used in reporting) ---
# Minimum improvement vs. clean to flag success.
CS_DELTA_MIN=1
# Absolute score threshold to flag success.
CS_ABS_MIN=9
